export type KeyType = {
  privateKey: Buffer;
  publicKey: Buffer;
  chainCode: Buffer;
};
export type WalletType = {
  privateKey: Buffer;
  publicKey: Buffer;
  chainCode: Buffer;
  walletName: string;
};
